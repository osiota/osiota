#!/bin/sh

SCRIPTPATH="$(readlink -f 2>/dev/null || perl -MCwd -e 'print Cwd::abs_path shift' $0)"

erconfig="config.json"
if test "x$1" != "x"
then
	erconfig="$1.json"
fi

"${SCRIPTPATH}/osiota.js" --config "${erconfig}"

